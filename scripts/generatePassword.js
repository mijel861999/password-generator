const generatePassword = (options) => {
  // TODO: generate password with the options

  const password = "";
  const {
    characterLenght,
    hasLowercase,
    hasUppercase,
    hasNumbers,
    hasSymbols,
  } = options;

  return password;
};

export default generatePassword;
